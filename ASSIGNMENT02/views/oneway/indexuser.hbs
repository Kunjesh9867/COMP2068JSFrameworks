<!-- FILEPATH -->
<!-- This code represents a selection of code that needs documentation. -->
<!-- Please add appropriate documentation comments here. -->
<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Document metadata -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <!-- Internal styles -->
    <style>
        /* General styling for the body */
        body {
            /* Specify font family and weight for the body text */
        }

        /* Styling for individual sections */
        section {
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            font-weight: 500;
            max-width: 60%;
            max-height: 150px;
            margin: 2em auto;
            padding: 15px;
            background-color: wheat;
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Styling for specific elements within a section */
        div .time {
            font-size: 25px;
        }

        div .nonstop {
            padding-left: 10%;
            font-size: 15px;
        }

        div .price {
            padding-left: 20%;
            font-size: 25px;
        }

        div .city {
            margin: 0px;
            padding: 0px;
        }

        div .date {
            padding-left: 15%;
        }

        div .airline {
            padding-left: 20%;
            font-size: 20px;
        }

        /* Styling for buttons and links */
        .btn {
            padding-left: 90%;
        }

        i {
            text-decoration: none;
        }

        #xx {
            text-decoration: none;
            color: darkorange;
        }

        .xx {
            text-decoration: none;
            color: yellow;
        }

        .xx:hover {
            text-decoration: underline;
            color: blue;
        }

        a {
            padding-right: 10px;
        }

        /* Styling for the search form */
        .search-form {
            display: flex;
            margin-top: 20px;
        }

        .search-input {
            padding: 10px;
            font-size: 25px;
            border: 1px solid #ccc;
            border-radius: 5px 0 0 5px;
            outline: none;
            width: 500px;
        }

        .search-button {
            background-color: purple;
            color: #fff;
            border: 1px solid purple;
            padding: 10px 15px;
            border-radius: 0 5px 5px 0;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 100px;
            text-align: center;
        }

        .search-button:hover {
            background-color: darkblue;
        }

        .searchbtn {
            padding-left: 30%;
        }
    </style>
</head>

<!-- Heading and search form for the page -->
<body>
    <h1 style="text-align: center;" id="xx">Book Flight</h1>
    <div class="searchbtn">
        <form action="" class="search-form">
            <input type="text" placeholder="Search by City [ YYZ, Toronto, ATH ]" name="search" class="search-input">
            <button type="submit" class="search-button">Search</button>
        </form>
    </div>

    <!-- Iterate through the dataset and display flight information -->
    {{#each dataset}}
    <section>
        <!-- Display time, nonstop status, and price -->
        <div>
            <span class="time">{{this.time}}</span>
            <span class="nonstop">{{this.nonstop}}</span>
            <span class="price">CA ${{this.price}}</span>
        </div>
        <!-- Display city, date, airline, and add to cart button -->
        <div>
            <span class="city">{{this.city}}</span>
            <span class="date">{{this.date}}</span>
            <span class="airline">{{this.flight}}</span>
            <span class="btn"><a href="/cart/add/{{this._id}}"><i class="fa-solid fa-plane-circle-check fa-2xl" style="color:darkorchid;"></i></a></span>
        </div>
    </section>
    {{/each}}
</body>

</html>
